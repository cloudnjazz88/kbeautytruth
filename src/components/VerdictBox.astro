---
interface Props {
  verdict?: string;
}

const { verdict } = Astro.props;
---

{
  verdict && (
    <section class="verdict" aria-labelledby="verdict-title">
      <p class="snapshot-kicker">The short answer</p>
      <h2 class="verdict-title" id="verdict-title">My verdict</h2>
      <p>{verdict}</p>
    </section>
  )
}
